Below is a draft of stand-up notes under `## Draft summary`, followed by the source notes under
`## Section extracts`. Return the full corrected Markdown document, starting directly with its
`#` title. Do not return a comment list or an explanation, and do not echo either wrapper
heading.

Check completeness: every person who gave an update appears exactly once, with everything they
said folded into their block; every blocker, announcement, follow-up, ticket number, and date in
the extracts is present; every blocker also has a row in the Blockers table. The extracts
overlap, so an update repeated across them still appears only once.

Check accuracy: no invented updates or blockers, no update attributed to the wrong person, no
work marked done that was only in progress.

Keep the structure (title, one-line summary, Team Themes, Updates, Blockers table,
Announcements, Shoutouts) and keep it terse. Merge any duplicate person heading into one block.
Never shorten or delete correct content, and never add paragraphs of narrative, a closing
assessment, or a cutoff disclaimer. If the draft is already correct, return it unchanged aside
from necessary fixes.
