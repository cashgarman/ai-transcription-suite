Below is a two-host script under `## Draft summary`, followed by the source material under
`## Section extracts`. Return the full corrected script, starting directly with its first host
line. Do not return a comment list or an explanation, and do not echo either wrapper heading.

Check that every substantive topic, decision, number, name, and disagreement in the source is
somewhere in the script, that nothing was invented, and that statements are attributed to the
people who actually made them.

Check the form: only `**Host A:**` and `**Host B:**` lines, no headings, no bullet lists, no
stage directions or bracketed cues, no claim that the recording was cut off, and figures
written the way a voice would say them.

Add missing substance as new host lines and fix wrong attributions. Never shorten the script or
delete correct content. If it is already correct, return it unchanged.
