Join the script segments in the numbered `### Section` blocks below into one continuous
two-host script.

This is a stitching pass, not a rewrite. Keep every line, in order, in the form
`**Host A:** ...` or `**Host B:** ...`. The only changes you may make are to repair a line that
was split across a section boundary, to remove a duplicated line, and to fix the speaker so the
hosts alternate sensibly across a boundary.

Do not add a title, headings, stage directions, or new dialogue. Do not shorten or summarize,
and do not copy a `### Section N` heading into the output.

Output the joined script and nothing else: no preamble and no code fence.
