Below is a draft front matter under `## Draft summary`, followed by the source notes under
`## Section extracts`. Return the full corrected Markdown front matter, starting directly with
its `#` title. Do not return a comment list or an explanation, and do not echo either wrapper
heading.

Check completeness of the overview sections only: named participants, visual direction and the
adjectives used, every reference named, every asset, character, or shot discussed, every piece
of feedback and who gave it, approvals and rejections, decisions, action items, open questions,
risks, dependencies, blockers, numbers, and deadlines that appear in the extracts must be
present. The extracts overlap, so an item repeated across them still appears only once.

Check accuracy: no invented references or critiques, no dropped owners, no contradicted
decisions, no piece marked approved that was not, and no cutoff or attribution disclaimers
unless they are truly warranted.

Keep the front-matter structure (title, one-line summary, participants, primary topics,
executive summary, visual direction, references, assets, feedback and approvals, key decisions,
action items table, open questions, risks, closing assessment). Add missing content and fix
wrong attributions. Never shorten or delete existing correct content, and do not add detailed
topic sections; the detailed discussion is appended separately. If the draft is already
correct, return it unchanged aside from necessary fixes.
