# Create a read-only token at https://huggingface.co/settings/tokens.
# Accept the model terms for BOTH gated models before running diarization:
#   https://huggingface.co/pyannote/speaker-diarization-3.1
#   https://huggingface.co/pyannote/segmentation-3.0
HF_TOKEN=

# If model downloads fail with WinError 10054 or SSL/certificate errors on Windows,
# set this to 1 for the first run, then remove it after models are cached locally.
# HF_HUB_INSECURE_SSL=1
